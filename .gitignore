# ─────────────────────────────────────────────────────────────────────────────
# qw-edit — git 忽略规则
#
# `.unpack/` 必须排除，且**必须保留在本地**：
#   · 它是千问办公 (QwenWork) **客户端自己的渲染器资源**（实测 376 MB / 28,267 文件），
#     不是本项目的源码，也不是我们的分发物；
#   · 把它提交/发布 = 把**第三方客户端的资源**重新分发出去 —— 体积上会撑爆仓库，
#     许可/ToS 上也不干净（那是别人的代码与资源）；
#   · 它同时是本地研究素材（逆向/调试用），所以**不删**，只是**不公开**。
#   ⚠️ 本文件是「不公开」的唯一强制点：`git` 靠它，`npm` 靠 package.json 的 `files`
#      （只有 bin/ 与 src/）。两处都不要放开。
# ─────────────────────────────────────────────────────────────────────────────

# 客户端资源（第三方；376MB / 28k 文件）—— 绝不公开，也绝不删除
.unpack/

# 开发期 CDP 回归脚本（16 个）：**含本机绝对路径**（/Users/<user>/…），
# 直接提交会把本机用户名/目录结构带进公开仓 ⇒ 先留在本地。
# 若要公开：先把这些路径改成占位/环境变量，再把本行删掉。
tools/

# 发布工作文档（内部流程记录，非产品文档）
PUBLISH-CHECKLIST.md
PUBLISH-READINESS-REPORT.md

# 依赖与构建产物
node_modules/

# 日志与系统杂项
*.log
logs/
.DS_Store

# 本地打包/调试产物
*.tgz
.qw-edit/
coverage/

# 编辑器
.idea/
.vscode/
